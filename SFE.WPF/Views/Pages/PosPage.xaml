<UserControl x:Class="SFE.WPF.Views.Pages.PosPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <!--  ★ Override MaterialDesign's scrollbar/scrollviewer with our thin ones  -->
        <Style BasedOn="{StaticResource ThinScrollBarStyle}"
               TargetType="ScrollBar" />
        <Style BasedOn="{StaticResource FadeScrollViewerStyle}"
               TargetType="ScrollViewer" />

        <!--  ══════════ CARD ══════════  -->
        <Style x:Key="PosCard"
               TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="14"
                                      Opacity="0.06"
                                      ShadowDepth="2"
                                      Color="#0D2B3E" />
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ OVERLAY CARD ══════════  -->
        <Style x:Key="PosOverlayCard"
               TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="28"
                                      Opacity="0.14"
                                      ShadowDepth="6"
                                      Color="#0D2B3E" />
                </Setter.Value>
            </Setter>
        </Style>

        <DropShadowEffect x:Key="PopupShadow"
                          BlurRadius="24"
                          Direction="270"
                          Opacity="0.12"
                          ShadowDepth="5"
                          Color="#0D2B3E" />

        <!--  ══════════ FIELD LABEL ══════════  -->
        <Style x:Key="PosFieldLabel"
               TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="Margin" Value="2,0,0,6" />
        </Style>

        <!--  ══════════ TOUCH TEXTBOX (min 48px) ══════════  -->
        <Style x:Key="PosTextBox"
               TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush">
                                    <Setter.Value>
                                        <SolidColorBrush Opacity="0.5"
                                                         Color="{StaticResource PrimaryColor}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ TOUCH COMBOBOX (min 48px) ══════════  -->
        <Style x:Key="PosComboBox"
               TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="MinHeight" Value="48" />
        </Style>

        <!--  ══════════ ICON BUTTON (touch 44×44) ══════════  -->
        <Style x:Key="PosIconBtn"
               TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="44" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F4F6" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E5E7EB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ CLOSE BUTTON (touch 40×40) ══════════  -->
        <Style x:Key="PosCloseBtn"
               TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="ToolTip" Value="Fermer" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Width="40"
                                Height="40"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <materialDesign:PackIcon Width="18"
                                                     Height="18"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Foreground="{StaticResource TextSecondaryBrush}"
                                                     Kind="Close" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FEE2E2" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FECACA" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ MINI ACTION BUTTON ══════════  -->
        <Style x:Key="PosMiniBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#37B1E4" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#1A8CBF"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#37B1E4"
                                                        Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ DANGER BUTTON ══════════  -->
        <Style x:Key="PosDangerBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#DC2626" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#FEE2E2" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#FECACA"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#FEE2E2"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ SUCCESS BUTTON ══════════  -->
        <Style x:Key="PosSuccessBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#065F46" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#DCFCE7" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#BBF7D0"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#DCFCE7"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ PURPLE BUTTON ══════════  -->
        <Style x:Key="PosPurpleBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#7C3AED" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#6D28D9"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#7C3AED"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ ACCENT (red) BUTTON ══════════  -->
        <Style x:Key="PosAccentBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="{StaticResource AccentColor}" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#DC0F0F"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="{StaticResource AccentColor}"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ GHOST BUTTON ══════════  -->
        <Style x:Key="PosGhostBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#F3F4F6" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#E5E7EB"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#F3F4F6"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ ENCAISSER (big gradient) ══════════  -->
        <Style x:Key="PosProcessBtn"
               TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,16" />
            <Setter Property="MinHeight" Value="58" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="14">
                            <Border.Background>
                                <LinearGradientBrush x:Name="BdGrad" StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop x:Name="G0" Offset="0" Color="#10B981" />
                                    <GradientStop x:Name="G1" Offset="1" Color="#059669" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16"
                                                  Opacity="0.3"
                                                  ShadowDepth="3"
                                                  Color="#10B981" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#059669"
                                                        Duration="0:0:0.18" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#047857"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#10B981"
                                                        Duration="0:0:0.22" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#059669"
                                                        Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ HOLD BUTTON (amber) ══════════  -->
        <Style x:Key="PosHoldBtn"
               TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,14" />
            <Setter Property="MinHeight" Value="58" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="14">
                            <Border.Background>
                                <LinearGradientBrush x:Name="BdGrad" StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop x:Name="G0" Offset="0" Color="#F59E0B" />
                                    <GradientStop x:Name="G1" Offset="1" Color="#D97706" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12"
                                                  Opacity="0.25"
                                                  ShadowDepth="2"
                                                  Color="#F59E0B" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#D97706"
                                                        Duration="0:0:0.18" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#B45309"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#F59E0B"
                                                        Duration="0:0:0.22" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#D97706"
                                                        Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ PRIMARY GRADIENT BUTTON ══════════  -->
        <Style x:Key="PosPrimaryBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="MinHeight" Value="54" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush x:Name="BdGrad" StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop x:Name="G0" Offset="0" Color="#37B1E4" />
                                    <GradientStop x:Name="G1" Offset="1" Color="#1A8CBF" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8"
                                                  Opacity="0.18"
                                                  ShadowDepth="1"
                                                  Color="#37B1E4" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#2DA0D3"
                                                        Duration="0:0:0.18" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#1579A8"
                                                        Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="G0"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#37B1E4"
                                                        Duration="0:0:0.22" />
                                        <ColorAnimation Storyboard.TargetName="G1"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#1A8CBF"
                                                        Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ QUICK AMOUNT CHIP ══════════  -->
        <Style x:Key="PosChipBtn"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="{StaticResource BackgroundColor}" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="{StaticResource BorderColor}"
                                                        Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="{StaticResource BackgroundColor}"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ DISCOUNT CHIP ══════════  -->
        <Style x:Key="PosDiscountChip"
               TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#DC2626" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#FEE2E2" />
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#FECACA"
                                                        Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BdBg"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#FEE2E2"
                                                        Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ PRODUCT CARD ══════════  -->
        <Style x:Key="ProductCardBorder"
               TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4"
                                      Opacity="0.04"
                                      ShadowDepth="1"
                                      Color="#0D2B3E" />
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ POPUP ITEM BUTTON ══════════  -->
        <Style x:Key="PopupItemBtn"
               TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="MinHeight" Value="52" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,1">
                            <Border.Background>
                                <SolidColorBrush x:Name="BdBg"
                                                 Color="#00000000" />
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                            </Border.BorderBrush>
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Opacity="0.04"
                                                         Color="{StaticResource PrimaryColor}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ══════════ OVERLAY HEADER ══════════  -->
        <Style x:Key="OverlayHeader"
               TargetType="Border">
            <Setter Property="Padding" Value="28,20,28,16" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"
                              MinWidth="440" />
            <ColumnDefinition Width="460" />
        </Grid.ColumnDefinitions>

        <!--  ══════════════════════════════════════════════════════  -->
        <!--  LEFT: Catalogue  -->
        <!--  ══════════════════════════════════════════════════════  -->
        <DockPanel Grid.Column="0"
                   Margin="20,16,10,16">

            <!--  TOP BAR  -->
            <Border Margin="0,0,0,16"
                    Padding="22,16"
                    DockPanel.Dock="Top"
                    Style="{StaticResource PosCard}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center"
                                    ToolTip="Heure et date actuelles">
                            <TextBlock FontFamily="Consolas"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource PrimaryBrush}"
                                       Text="{Binding CurrentTime}"
                                       ToolTip="{Binding CurrentTime}" />
                            <TextBlock Margin="0,2,0,0"
                                       FontFamily="{StaticResource AppFont}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Text="{Binding CurrentDate}"
                                       ToolTip="{Binding CurrentDate}" />
                        </StackPanel>

                        <Border Grid.Column="1"
                                Margin="16,0,0,0"
                                Padding="12,6"
                                VerticalAlignment="Center"
                                CornerRadius="8"
                                ToolTip="Mode de tarification actuel (TTC ou HT)">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <SolidColorBrush Opacity="0.1"
                                                             Color="{StaticResource PrimaryColor}" />
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsHtMode}"
                                                     Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Opacity="0.12"
                                                                     Color="{StaticResource WarningColor}" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="14"
                                                         Height="14"
                                                         Margin="0,0,5,0"
                                                         VerticalAlignment="Center">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Kind" Value="TagOutline" />
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHtMode}"
                                                             Value="True">
                                                    <Setter Property="Foreground" Value="#92400E" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Text="{Binding PriceModeDisplay}"
                                           ToolTip="{Binding PriceModeDisplay}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHtMode}"
                                                             Value="True">
                                                    <Setter Property="Foreground" Value="#92400E" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2"
                                MinHeight="44"
                                Margin="18,0"
                                Padding="14,0"
                                Background="{StaticResource BackgroundBrush}"
                                CornerRadius="10"
                                ToolTip="Rechercher un produit par nom ou code">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <materialDesign:PackIcon Width="18"
                                                         Height="18"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource TextMutedBrush}"
                                                         Kind="Magnify" />
                                <TextBox x:Name="SearchBox"
                                         Grid.Column="1"
                                         Padding="0,10"
                                         VerticalAlignment="Center"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontFamily="{StaticResource AppFont}"
                                         FontSize="14"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=250}"
                                         ToolTip="Tapez pour rechercher un produit" />
                                <Popup Grid.Column="1"
                                       MinWidth="400"
                                       AllowsTransparency="True"
                                       IsOpen="{Binding ShowSearchResults, Mode=TwoWay}"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=SearchBox}"
                                       PopupAnimation="Fade"
                                       StaysOpen="False">
                                    <Border MinWidth="400"
                                            MaxWidth="600"
                                            MaxHeight="360"
                                            Background="White"
                                            BorderThickness="1"
                                            CornerRadius="0,0,12,12"
                                            Effect="{StaticResource PopupShadow}">
                                        <Border.BorderBrush>
                                            <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                                        </Border.BorderBrush>
                                        <ScrollViewer PanningMode="VerticalOnly"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding SearchResults}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource PopupItemBtn}"
                                                                ToolTip="{Binding Name}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <StackPanel VerticalAlignment="Center">
                                                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                                                               FontSize="13"
                                                                               FontWeight="Medium"
                                                                               Text="{Binding Name}"
                                                                               TextTrimming="CharacterEllipsis"
                                                                               ToolTip="{Binding Name}" />
                                                                    <TextBlock Margin="0,2,0,0"
                                                                               FontFamily="Consolas"
                                                                               FontSize="11"
                                                                               Foreground="{StaticResource TextMutedBrush}"
                                                                               ToolTip="{Binding Code}">
                                                                        <Run Text="{Binding Code, Mode=OneWay}" />
                                                                    </TextBlock>
                                                                </StackPanel>
                                                                <Border Grid.Column="1"
                                                                        Margin="12,0,0,0"
                                                                        Padding="10,4"
                                                                        VerticalAlignment="Center"
                                                                        CornerRadius="6"
                                                                        ToolTip="Prix unitaire TTC en CDF">
                                                                    <Border.Background>
                                                                        <SolidColorBrush Opacity="0.08"
                                                                                         Color="{StaticResource PrimaryColor}" />
                                                                    </Border.Background>
                                                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                                                               FontSize="13"
                                                                               FontWeight="Bold"
                                                                               Foreground="{StaticResource PrimaryBrush}"
                                                                               Text="{Binding UnitPriceTtcCdf, StringFormat='{}{0:N2} CDF'}" />
                                                                </Border>
                                                            </Grid>
                                                        </Button>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="3"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    ToolTip="Résumé des ventes du jour">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="14"
                                                         Height="14"
                                                         Margin="0,0,6,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource TextMutedBrush}"
                                                         Kind="ChartBarStacked" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="12"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           ToolTip="Nombre de ventes aujourd'hui">
                                    <Run Text="{Binding TodaySalesCount, Mode=OneWay}" />
                                    <Run Text=" ventes" />
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Margin="0,2,0,0"
                                       HorizontalAlignment="Right"
                                       FontFamily="{StaticResource AppFont}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryBrush}"
                                       ToolTip="Total des ventes du jour en CDF">
                                <Run Text="{Binding TodaySalesTotal, StringFormat=N2, Mode=OneWay}" />
                                <Run Text=" CDF" />
                            </TextBlock>
                        </StackPanel>
                    </Grid>

                    <Border Margin="0,14,0,0"
                            Padding="14,10"
                            BorderThickness="1"
                            CornerRadius="10"
                            ToolTip="Point de vente sélectionné pour cette session">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Opacity="0.05"
                                                         Color="{StaticResource PrimaryColor}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="BorderBrush">
                                    <Setter.Value>
                                        <SolidColorBrush Opacity="0.2"
                                                         Color="{StaticResource PrimaryColor}" />
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPointOfSale}"
                                                 Value="{x:Null}">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <SolidColorBrush Opacity="0.05"
                                                                 Color="{StaticResource ErrorColor}" />
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="BorderBrush">
                                            <Setter.Value>
                                                <SolidColorBrush Opacity="0.2"
                                                                 Color="{StaticResource ErrorColor}" />
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="16"
                                                         Height="16"
                                                         Margin="0,0,8,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource PrimaryDarkBrush}"
                                                         Kind="StorefrontOutline" />
                                <Border Margin="0,0,8,0"
                                        Padding="6,2"
                                        VerticalAlignment="Center"
                                        Background="{StaticResource PrimaryLightBrush}"
                                        CornerRadius="4"
                                        ToolTip="Code du point de vente"
                                        Visibility="{Binding SelectedPointOfSale, Converter={StaticResource IsNotNullToVisibilityConverter}}">
                                    <TextBlock FontFamily="Consolas"
                                               FontSize="10"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource PrimaryDarkBrush}"
                                               Text="{Binding SelectedPointOfSale.Code}"
                                               ToolTip="{Binding SelectedPointOfSale.Code}" />
                                </Border>
                                <TextBlock VerticalAlignment="Center"
                                           FontFamily="{StaticResource AppFont}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Text="{Binding SelectedPointOfSale.Name}"
                                           ToolTip="{Binding SelectedPointOfSale.Name}" />
                            </StackPanel>
                            <TextBlock Margin="24,3,0,0"
                                       FontFamily="{StaticResource AppFont}"
                                       FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Text="{Binding SelectedPosInfo}"
                                       ToolTip="{Binding SelectedPosInfo}"
                                       Visibility="{Binding SelectedPointOfSale, Converter={StaticResource IsNotNullToVisibilityConverter}}" />
                            <StackPanel Margin="24,3,0,0"
                                        Orientation="Horizontal"
                                        ToolTip="Erreur : Aucun point de vente n'est configuré. Veuillez configurer un point de vente dans les paramètres.">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedPointOfSale}"
                                                         Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <materialDesign:PackIcon Width="13"
                                                         Height="13"
                                                         Margin="0,0,5,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="#DC2626"
                                                         Kind="AlertOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="11"
                                           FontStyle="Italic"
                                           Foreground="#DC2626"
                                           Text="Aucun point de vente configuré"
                                           ToolTip="Erreur : Aucun point de vente n'est configuré. Veuillez en sélectionner un dans les paramètres pour pouvoir effectuer des ventes." />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>


            <!--  CATEGORIES  -->
            <ScrollViewer Margin="0,0,0,16"
                          ClipToBounds="False"
                          DockPanel.Dock="Top"
                          HorizontalScrollBarVisibility="Auto"
                          PanningMode="HorizontalOnly"
                          VerticalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,4,0,8"
                            Orientation="Horizontal">

                    <Button MinHeight="68"
                            Margin="0,0,10,0"
                            Padding="28,18"
                            BorderThickness="0"
                            Command="{Binding ShowFavoritesCommand}"
                            Cursor="Hand"
                            FontFamily="{StaticResource AppFont}"
                            FontSize="15"
                            FontWeight="SemiBold"
                            ToolTip="Afficher uniquement les produits favoris">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource CardBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="CatBd"
                                                    Padding="{TemplateBinding Padding}"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1.5"
                                                    CornerRadius="10">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="4"
                                                                      Opacity="0.04"
                                                                      ShadowDepth="1"
                                                                      Color="#0D2B3E" />
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="CatBd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowFavoritesOnly}"
                                                 Value="True">
                                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Width="15"
                                                     Height="15"
                                                     Margin="0,0,6,0"
                                                     VerticalAlignment="Center"
                                                     Kind="StarOutline" />
                            <TextBlock Text="Favoris" />
                        </StackPanel>
                    </Button>

                    <Button MinHeight="68"
                            Margin="0,0,10,0"
                            Padding="28,18"
                            Background="{StaticResource CardBrush}"
                            BorderThickness="0"
                            Command="{Binding ShowAllProductsCommand}"
                            Content="Tout"
                            Cursor="Hand"
                            FontFamily="{StaticResource AppFont}"
                            FontSize="15"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            ToolTip="Afficher tous les produits" />

                    <ItemsControl ItemsSource="{Binding Categories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button MinHeight="68"
                                        Margin="0,0,10,0"
                                        Padding="28,18"
                                        Background="{StaticResource CardBrush}"
                                        BorderThickness="0"
                                        Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand"
                                        FontFamily="{StaticResource AppFont}"
                                        FontSize="15"
                                        Foreground="{StaticResource TextPrimaryBrush}"
                                        ToolTip="{Binding Name}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Margin="0,0,6,0"
                                                   Text="{Binding Icon}" />
                                        <TextBlock Text="{Binding Name}" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!--  PRODUCT GRID  -->
            <ScrollViewer PanningMode="VerticalOnly"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DisplayProducts}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="160"
                                    Height="118"
                                    Margin="0,0,12,12"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Cursor="Hand"
                                    ToolTip="{Binding Name}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="CardBorder"
                                                Style="{StaticResource ProductCardBorder}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <TextBlock MaxHeight="34"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="12"
                                                           FontWeight="Medium"
                                                           Text="{Binding Name}"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="Wrap"
                                                           ToolTip="{Binding Name}" />
                                                <TextBlock Grid.Row="1"
                                                           VerticalAlignment="Center"
                                                           FontFamily="Consolas"
                                                           FontSize="10"
                                                           Foreground="{StaticResource TextMutedBrush}"
                                                           ToolTip="{Binding Code}">
                                                    <Run Text="{Binding Code, Mode=OneWay}" />
                                                </TextBlock>
                                                <Grid Grid.Row="2">
                                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                                               FontSize="15"
                                                               FontWeight="Bold"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               ToolTip="Prix unitaire TTC en CDF">
                                                        <Run Text="{Binding UnitPriceTtcCdf, StringFormat=N2, Mode=OneWay}" />
                                                    </TextBlock>
                                                    <TextBlock HorizontalAlignment="Right"
                                                               VerticalAlignment="Bottom"
                                                               FontFamily="{StaticResource AppFont}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               ToolTip="Groupe de taxe applicable">
                                                        <Run Text="{Binding TaxGroup, Mode=OneWay}" />
                                                    </TextBlock>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                <Setter TargetName="CardBorder" Property="Background">
                                                    <Setter.Value>
                                                        <SolidColorBrush Opacity="0.06"
                                                                         Color="{StaticResource PrimaryColor}" />
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                            <EventTrigger RoutedEvent="Button.Click">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation AutoReverse="True"
                                                                        Storyboard.TargetName="CardBorder"
                                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                        To="#37B1E4"
                                                                        Duration="0:0:0.08" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>

        <!--  ══════════════════════════════════════════════════════  -->
        <!--  RIGHT: Cart (460px wide)  -->
        <!--  ══════════════════════════════════════════════════════  -->
        <Grid Grid.Column="1"
              Margin="8,16,20,16">

            <Border CornerRadius="16"
                    Style="{StaticResource PosCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="1.8*" />
                    </Grid.RowDefinitions>

                    <!--  ────── ROW 0: CART HEADER ──────  -->
                    <Border Grid.Row="0"
                            Padding="28,20,28,16"
                            BorderThickness="0,0,0,1">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                        </Border.BorderBrush>
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid>
                                    <StackPanel VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <Grid ToolTip="Nombre d'articles dans le panier">
                                            <materialDesign:PackIcon x:Name="CartIcon"
                                                                     Width="22"
                                                                     Height="22"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="{StaticResource PrimaryBrush}"
                                                                     Kind="CartOutline" />

                                            <!--  Badge  -->
                                            <Border x:Name="CartBadge"
                                                    Height="18"
                                                    MinWidth="18"
                                                    Margin="0,-6,-8,0"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Top"
                                                    Background="#EF4444"
                                                    CornerRadius="10"
                                                    RenderTransformOrigin="0.5,0.5"
                                                    Visibility="{Binding CartItemCount, Converter={StaticResource IntToVisibilityConverter}}">
                                                <Border.RenderTransform>
                                                    <ScaleTransform x:Name="BadgeScale" ScaleX="1" ScaleY="1" />
                                                </Border.RenderTransform>

                                                <TextBlock HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           Foreground="White"
                                                           Text="{Binding CartItemCount}" />
                                            </Border>
                                        </Grid>

                                        <TextBlock Margin="12,0,0,0"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="18"
                                                   FontWeight="Bold"
                                                   Text="Panier"
                                                   ToolTip="Panier de la vente en cours" />
                                    </StackPanel>
                                </Grid>
                                <StackPanel Grid.Column="2"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                    <Button Command="{Binding ToggleClientPanelCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Sélectionner ou gérer le client">
                                        <materialDesign:PackIcon Width="19"
                                                                 Height="19"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 Kind="AccountOutline" />
                                    </Button>
                                    <Button Command="{Binding ToggleCommentPanelCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Ajouter des commentaires à la facture">
                                        <materialDesign:PackIcon Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 Kind="CommentTextOutline" />
                                    </Button>
                                    <Button Command="{Binding ToggleDiscountPanelCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Appliquer une remise sur l'article sélectionné"
                                            Visibility="{Binding IsNormalized, Converter={StaticResource InverseBoolConverter}}">
                                        <materialDesign:PackIcon Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource AccentBrush}"
                                                                 Kind="PercentOutline" />
                                    </Button>
                                    <Button Command="{Binding ToggleHeldPanelCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Voir les ventes mises en attente">
                                        <Grid>
                                            <materialDesign:PackIcon Width="18"
                                                                     Height="18"
                                                                     Foreground="{StaticResource TextSecondaryBrush}"
                                                                     Kind="PauseCircleOutline" />
                                            <Border Height="16"
                                                    MinWidth="16"
                                                    Margin="0,-4,-6,0"
                                                    Padding="4,0"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Top"
                                                    Background="#EF4444"
                                                    CornerRadius="8"
                                                    Visibility="{Binding HeldCount, Converter={StaticResource IntToVisibilityConverter}}">
                                                <TextBlock HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           FontSize="9"
                                                           FontWeight="Bold"
                                                           Foreground="White"
                                                           Text="{Binding HeldCount}" />
                                            </Border>
                                        </Grid>
                                    </Button>
                                    <Button Command="{Binding TogglePendingPanelCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Voir les factures en attente de normalisation">
                                        <materialDesign:PackIcon Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 Kind="TimerSandEmpty" />
                                    </Button>
                                    <Button Command="{Binding ClearCartCommand}"
                                            Style="{StaticResource PosIconBtn}"
                                            ToolTip="Vider tout le panier"
                                            Visibility="{Binding IsNormalized, Converter={StaticResource InverseBoolConverter}}">
                                        <materialDesign:PackIcon Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource TextMutedBrush}"
                                                                 Kind="TrashCanOutline" />
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <Grid>
                                <TextBlock VerticalAlignment="Center"
                                           FontFamily="{StaticResource AppFont}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           ToolTip="Nombre de lignes et d'articles dans le panier">
                                    <Run Text="{Binding CartItemCount, Mode=OneWay}" />
                                    <Run Text=" ligne(s) ·" />
                                    <Run Text="{Binding TotalArticles, Mode=OneWay}" />
                                    <Run Text=" art." />
                                </TextBlock>
                                <TextBlock HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           FontFamily="Consolas"
                                           FontSize="10"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Text="{Binding InvoiceNumber}"
                                           ToolTip="{Binding InvoiceNumber}" />
                            </Grid>

                            <!--  Client badge  -->
                            <Border MinHeight="42"
                                    Margin="0,12,0,0"
                                    Padding="14,10"
                                    CornerRadius="10"
                                    ToolTip="Client associé à cette vente"
                                    Visibility="{Binding HasClientSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Border.Background>
                                    <SolidColorBrush Opacity="0.06"
                                                     Color="{StaticResource PrimaryColor}" />
                                </Border.Background>
                                <Grid>
                                    <StackPanel VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="15"
                                                                 Height="15"
                                                                 Margin="0,0,8,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="{StaticResource PrimaryBrush}"
                                                                 Kind="AccountCheckOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   Text="{Binding ClientName}"
                                                   ToolTip="{Binding ClientName}" />
                                        <TextBlock Margin="8,0,0,0"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="{Binding ClientTypeMention}"
                                                   ToolTip="{Binding ClientTypeMention}" />
                                    </StackPanel>
                                    <Button Width="36"
                                            Height="36"
                                            HorizontalAlignment="Right"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{Binding ClearClientCommand}"
                                            Cursor="Hand"
                                            ToolTip="Retirer le client de cette vente">
                                        <materialDesign:PackIcon Width="14"
                                                                 Height="14"
                                                                 Foreground="#DC2626"
                                                                 Kind="Close" />
                                    </Button>
                                </Grid>
                            </Border>

                            <!--  Credit note / Advance badges  -->
                            <StackPanel Margin="0,8,0,0"
                                        Orientation="Horizontal">
                                <Border MinHeight="36"
                                        Margin="0,0,8,0"
                                        Padding="12,7"
                                        CornerRadius="8"
                                        ToolTip="Cette vente est un avoir (note de crédit)"
                                        Visibility="{Binding IsCreditNote, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Border.Background>
                                        <SolidColorBrush Opacity="0.08"
                                                         Color="{StaticResource ErrorColor}" />
                                    </Border.Background>
                                    <Button Padding="0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{Binding ToggleCreditNotePanelCommand}"
                                            Cursor="Hand"
                                            ToolTip="Cliquer pour gérer les paramètres de l'avoir">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,6,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#DC2626"
                                                                     Kind="FileUndoOutline" />
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="#DC2626"
                                                       Text="Avoir" />
                                        </StackPanel>
                                    </Button>
                                </Border>
                                <Border MinHeight="36"
                                        Padding="12,7"
                                        CornerRadius="8"
                                        ToolTip="Cette vente est un acompte"
                                        Visibility="{Binding IsAdvanceInvoice, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Border.Background>
                                        <SolidColorBrush Opacity="0.08"
                                                         Color="#7C3AED" />
                                    </Border.Background>
                                    <Button Padding="0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Command="{Binding ToggleAdvancePanelCommand}"
                                            Cursor="Hand"
                                            ToolTip="Cliquer pour gérer les paramètres de l'acompte">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,6,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#7C3AED"
                                                                     Kind="CashClock" />
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="#7C3AED"
                                                       Text="Acompte" />
                                        </StackPanel>
                                    </Button>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!--  ────── ROW 1: CART ITEMS LIST ──────  -->
                    <ListBox Grid.Row="1"
                             Margin="0"
                             Padding="0"
                             HorizontalContentAlignment="Stretch"
                             Background="Transparent"
                             BorderThickness="0"
                             ItemsSource="{Binding CartItems}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             SelectedItem="{Binding SelectedCartItem}">
                        <ListBox.Template>
                            <ControlTemplate TargetType="ListBox">
                                <Border Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        SnapsToDevicePixels="True">
                                    <ScrollViewer Focusable="False"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  PanningMode="VerticalOnly"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ListBox.Template>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="20,3" />
                                <Setter Property="Cursor" Value="Hand" />
                                <Setter Property="MinHeight" Value="64" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Padding="18,14"
                                                    Background="{StaticResource BackgroundBrush}"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1.5"
                                                    CornerRadius="12">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                    <Setter TargetName="Bd" Property="Background">
                                                        <Setter.Value>
                                                            <SolidColorBrush Opacity="0.06"
                                                                             Color="{StaticResource PrimaryColor}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="BorderBrush">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="{StaticResource BorderColor}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid ToolTip="{Binding Name}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="13"
                                                   FontWeight="Medium"
                                                   Text="{Binding Name}"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding Name}" />
                                        <TextBlock Margin="0,2,0,0"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   ToolTip="Quantité et groupe de taxe">
                                            <Run Text="{Binding QuantityDisplay, Mode=OneWay}" />
                                            <Run Text=" · Grp " />
                                            <Run Text="{Binding TaxGroupLabel, Mode=OneWay}" />
                                        </TextBlock>
                                        <Border Margin="0,3,0,0"
                                                Padding="6,2"
                                                HorizontalAlignment="Left"
                                                CornerRadius="4"
                                                ToolTip="{Binding DiscountDisplay}"
                                                Visibility="{Binding HasDiscount, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Border.Background>
                                                <SolidColorBrush Opacity="0.08"
                                                                 Color="{StaticResource ErrorColor}" />
                                            </Border.Background>
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="10"
                                                       FontWeight="SemiBold"
                                                       Foreground="#DC2626"
                                                       Text="{Binding DiscountDisplay, Mode=OneWay}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Margin="10,0"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <Button Width="42"
                                                Height="42"
                                                Padding="0"
                                                Background="{StaticResource BackgroundBrush}"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.DecrementQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Cursor="Hand"
                                                ToolTip="Diminuer la quantité">
                                            <TextBlock HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Text="−" />
                                        </Button>
                                        <TextBox MinWidth="64"
                                                 Margin="8,0"
                                                 Padding="2,4"
                                                 VerticalAlignment="Center"
                                                 HorizontalContentAlignment="Center"
                                                 VerticalContentAlignment="Center"
                                                 Background="Transparent"
                                                 BorderBrush="{StaticResource BorderBrush}"
                                                 BorderThickness="0,0,0,1"
                                                 FontFamily="{StaticResource AppFont}"
                                                 FontSize="14"
                                                 FontWeight="Bold"
                                                 IsReadOnly="{Binding DataContext.IsNormalized, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Text="{Binding Quantity, StringFormat=N3, UpdateSourceTrigger=LostFocus, Mode=TwoWay, ValidatesOnExceptions=True}"
                                                 ToolTip="Quantité (max 3 décimales, ex. 3.587). Tab ou Entrée pour valider.">
                                            <TextBox.InputBindings>
                                                <!--
                                                    Entrée → déplace le focus, ce qui déclenche LostFocus
                                                    et donc la mise à jour de la source (Quantity).
                                                -->
                                                <KeyBinding Key="Enter"
                                                            Command="{x:Static ApplicationCommands.NotACommand}" />
                                            </TextBox.InputBindings>
                                        </TextBox>
                                        <Button Width="42"
                                                Height="42"
                                                Padding="0"
                                                Background="{StaticResource BackgroundBrush}"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.IncrementQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Cursor="Hand"
                                                ToolTip="Augmenter la quantité">
                                            <TextBlock HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Text="＋" />
                                        </Button>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                MinWidth="88"
                                                VerticalAlignment="Center">
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="14"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   Text="{Binding AmountTTC, StringFormat='{}{0:N2}'}"
                                                   ToolTip="Montant TTC de cette ligne (2 décimales)" />
                                        <Button MinHeight="26"
                                                Padding="0"
                                                HorizontalAlignment="Right"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Cursor="Hand"
                                                ToolTip="Retirer cet article du panier"
                                                Visibility="{Binding DataContext.IsNormalized, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBoolConverter}}">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Width="10"
                                                                         Height="10"
                                                                         Margin="0,0,3,0"
                                                                         VerticalAlignment="Center"
                                                                         Foreground="#EF4444"
                                                                         Kind="Close" />
                                                <TextBlock FontFamily="{StaticResource AppFont}"
                                                           FontSize="9"
                                                           Foreground="#EF4444"
                                                           Text="retirer" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!--  ────── ROW 2: CART FOOTER ──────  -->
                    <DockPanel Grid.Row="2">

                        <!--  Status messages  -->
                        <Border Margin="28,0,28,12"
                                Padding="14,10"
                                CornerRadius="8"
                                DockPanel.Dock="Bottom"
                                ToolTip="{Binding StatusMessage}"
                                Visibility="{Binding ShowSuccess, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Border.Background>
                                <SolidColorBrush Opacity="0.06"
                                                 Color="{StaticResource SuccessColor}" />
                            </Border.Background>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="14"
                                                         Height="14"
                                                         Margin="0,0,8,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource SuccessBrush}"
                                                         Kind="CheckCircleOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="11"
                                           Foreground="{StaticResource SuccessBrush}"
                                           Text="{Binding StatusMessage}"
                                           TextWrapping="Wrap"
                                           ToolTip="{Binding StatusMessage}" />
                            </StackPanel>
                        </Border>
                        <Border Margin="28,0,28,6"
                                Padding="14,10"
                                CornerRadius="8"
                                DockPanel.Dock="Bottom"
                                ToolTip="{Binding StatusMessage}"
                                Visibility="{Binding ShowError, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Border.Background>
                                <SolidColorBrush Opacity="0.06"
                                                 Color="{StaticResource ErrorColor}" />
                            </Border.Background>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="14"
                                                         Height="14"
                                                         Margin="0,0,8,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource ErrorBrush}"
                                                         Kind="AlertCircleOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="11"
                                           Foreground="{StaticResource ErrorBrush}"
                                           Text="{Binding StatusMessage}"
                                           TextWrapping="Wrap"
                                           ToolTip="{Binding StatusMessage}" />
                            </StackPanel>
                        </Border>

                        <!--  ── Scrollable totals + payment area ──  -->
                        <ScrollViewer PanningMode="VerticalOnly"
                                      VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="28,12,28,24">

                                <Separator Margin="0,0,0,14"
                                           Background="{StaticResource BorderLightBrush}" />

                                <!--  Discount sub-total  -->
                                <Grid MinHeight="28"
                                      Margin="0,0,0,8"
                                      ToolTip="Sous-total avant application des remises"
                                      Visibility="{Binding HasAnyDiscount, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Text="Sous-total avant remise" />
                                    <TextBlock HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Text="{Binding TotalHTBeforeDiscount, StringFormat='{}{0:N2}'}"
                                               TextDecorations="Strikethrough"
                                               ToolTip="{Binding TotalHTBeforeDiscount, StringFormat='{}{0:N2} CDF'}" />
                                </Grid>
                                <Grid MinHeight="28"
                                      Margin="0,0,0,8"
                                      ToolTip="Montant total de la remise appliquée"
                                      Visibility="{Binding HasAnyDiscount, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="12"
                                                                 Height="12"
                                                                 Margin="0,0,5,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="#DC2626"
                                                                 Kind="PercentOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="11"
                                                   Foreground="#DC2626"
                                                   Text="Remise" />
                                    </StackPanel>
                                    <TextBlock HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="#DC2626"
                                               Text="{Binding TotalDiscount, StringFormat='-{0:N2}'}"
                                               ToolTip="{Binding TotalDiscount, StringFormat='-{0:N2} CDF'}" />
                                </Grid>

                                <!--  HT  -->
                                <Grid MinHeight="30"
                                      Margin="0,0,0,8"
                                      ToolTip="Total Hors Taxes">
                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Text="HT" />
                                    <TextBlock HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Text="{Binding TotalHT, StringFormat='{}{0:N2}'}"
                                               ToolTip="{Binding TotalHT, StringFormat='{}{0:N2} CDF'}" />
                                </Grid>

                                <!--  TVA  -->
                                <Grid MinHeight="30"
                                      Margin="0,0,0,8"
                                      ToolTip="Taxe sur la Valeur Ajoutée">
                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               Text="TVA" />
                                    <TextBlock HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               Text="{Binding TotalTVA, StringFormat='{}{0:N2}'}"
                                               ToolTip="{Binding TotalTVA, StringFormat='{}{0:N2} CDF'}" />
                                </Grid>

                                <!--  Specific tax  -->
                                <Grid MinHeight="30"
                                      Margin="0,0,0,8"
                                      ToolTip="Montant de la taxe spécifique applicable"
                                      Visibility="{Binding HasAnySpecificTax, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="13"
                                                                 Height="13"
                                                                 Margin="0,0,5,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="#92400E"
                                                                 Kind="FlashOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="12"
                                                   Foreground="#92400E"
                                                   Text="Taxe spécifique" />
                                    </StackPanel>
                                    <TextBlock HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               Foreground="#92400E"
                                               Text="{Binding TotalSpecificTax, StringFormat='{}{0:N2}'}"
                                               ToolTip="{Binding TotalSpecificTax, StringFormat='{}{0:N2} CDF'}" />
                                </Grid>

                                <!--  ── Grand Total ──  -->
                                <Border Margin="0,10,0,10"
                                        Padding="22,18"
                                        CornerRadius="14"
                                        RenderTransformOrigin="0.5,0.5"
                                        ToolTip="Montant total de la facture">
                                    <Border.RenderTransform>
                                        <ScaleTransform x:Name="TotalScale" ScaleX="1" ScaleY="1" />
                                    </Border.RenderTransform>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Opacity="0.08"
                                                                     Color="{StaticResource PrimaryColor}" />
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHtMode}"
                                                             Value="True">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <SolidColorBrush Opacity="0.08"
                                                                             Color="{StaticResource WarningColor}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <TextBlock VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="15"
                                                   FontWeight="Bold"
                                                   Text="{Binding GrandTotalLabel}"
                                                   ToolTip="{Binding GrandTotalLabel}" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="22"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   ToolTip="{Binding GrandTotal, StringFormat='{}{0:N2} CDF'}">
                                            <Run Text="{Binding GrandTotal, StringFormat=N2, Mode=OneWay}" />
                                            <Run FontSize="14"
                                                 Text=" CDF" />
                                        </TextBlock>
                                    </Grid>
                                </Border>

                                <!--  ── Alternate currency ──  -->
                                <Border MinHeight="44"
                                        Margin="0,0,0,12"
                                        Padding="18,12"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        ToolTip="Équivalent du total dans la devise alternative">
                                    <Border.Background>
                                        <SolidColorBrush Color="{StaticResource BackgroundColor}" />
                                    </Border.Background>
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                                    </Border.BorderBrush>
                                    <Grid>
                                        <StackPanel VerticalAlignment="Center"
                                                    Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="15"
                                                                     Height="15"
                                                                     Margin="0,0,8,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="{StaticResource TextMutedBrush}"
                                                                     Kind="SwapHorizontal" />
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="11"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       ToolTip="{Binding AlternateCurrencyLabel}">
                                                <Run Text="{Binding AlternateCurrencyLabel, Mode=OneWay}" />
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="14"
                                                   FontWeight="Bold"
                                                   ToolTip="{Binding TotalInAlternateCurrency, StringFormat='{}{0:N2}'}">
                                            <Run Text="{Binding TotalInAlternateCurrency, StringFormat=N2, Mode=OneWay}" />
                                            <Run FontSize="9"
                                                 Text=" " />
                                            <Run FontSize="9"
                                                 Text="{Binding AlternateCurrencyLabel, Mode=OneWay}" />
                                        </TextBlock>
                                    </Grid>
                                </Border>

                                <!--  ── À payer TTC (HT mode) ──  -->
                                <Border Margin="0,0,0,12"
                                        Padding="20,14"
                                        CornerRadius="12"
                                        ToolTip="Montant total TTC à encaisser du client"
                                        Visibility="{Binding IsHtMode, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Border.Background>
                                        <SolidColorBrush Opacity="0.06"
                                                         Color="{StaticResource SuccessColor}" />
                                    </Border.Background>
                                    <Grid MinHeight="38">
                                        <StackPanel VerticalAlignment="Center"
                                                    Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="16"
                                                                     Height="16"
                                                                     Margin="0,0,8,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#065F46"
                                                                     Kind="CashMultiple" />
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"
                                                       Foreground="#065F46"
                                                       Text="À encaisser (TTC)" />
                                        </StackPanel>
                                        <TextBlock HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="#065F46"
                                                   ToolTip="{Binding TotalTTC, StringFormat='{}{0:N2} CDF'}">
                                            <Run Text="{Binding TotalTTC, StringFormat=N2, Mode=OneWay}" />
                                            <Run FontSize="11"
                                                 Text=" CDF" />
                                        </TextBlock>
                                    </Grid>
                                </Border>

                                <!--  ═══════ BEFORE NORMALIZATION ═══════  -->
                                <StackPanel Visibility="{Binding IsNormalized, Converter={StaticResource InverseBoolConverter}}">

                                    <!--  Payment type + Invoice type  -->
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <ComboBox ItemsSource="{Binding PaymentTypes}"
                                                  SelectedItem="{Binding SelectedPaymentType}"
                                                  Style="{StaticResource PosComboBox}"
                                                  ToolTip="Sélectionner le mode de paiement" />
                                        <ComboBox Grid.Column="2"
                                                  ItemsSource="{Binding InvoiceTypes}"
                                                  SelectedItem="{Binding InvoiceType}"
                                                  Style="{StaticResource PosComboBox}"
                                                  ToolTip="Sélectionner le type de facture" />
                                    </Grid>

                                    <!--  Split payment toggle  -->
                                    <Button Margin="0,0,0,14"
                                            HorizontalContentAlignment="Center"
                                            Command="{Binding ToggleSplitPaymentCommand}"
                                            Style="{StaticResource PosGhostBtn}"
                                            ToolTip="Diviser le paiement en plusieurs modes (espèces, carte, etc.)">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,8,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="{StaticResource TextSecondaryBrush}">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Kind" Value="CreditCardOutline" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ShowSplitPayment}"
                                                                         Value="True">
                                                                <Setter Property="Kind" Value="Close" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="12">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Paiement fractionné" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ShowSplitPayment}"
                                                                         Value="True">
                                                                <Setter Property="Text" Value="Fermer paiement fractionné" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Button>

                                    <!--  ── Split payment section ──  -->
                                    <StackPanel Visibility="{Binding ShowSplitPayment, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ItemsControl ItemsSource="{Binding PaymentItems}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid MinHeight="40"
                                                          Margin="0,4"
                                                          ToolTip="{Binding Label}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock VerticalAlignment="Center"
                                                                   FontFamily="{StaticResource AppFont}"
                                                                   FontSize="12"
                                                                   Text="{Binding Label}"
                                                                   ToolTip="{Binding Label}" />
                                                        <TextBlock Grid.Column="1"
                                                                   Margin="0,0,10,0"
                                                                   VerticalAlignment="Center"
                                                                   FontFamily="{StaticResource AppFont}"
                                                                   FontSize="12"
                                                                   FontWeight="SemiBold"
                                                                   Text="{Binding Amount, StringFormat=N2}"
                                                                   ToolTip="{Binding Amount, StringFormat='{}{0:N2} CDF'}" />
                                                        <Button Grid.Column="2"
                                                                Width="36"
                                                                Height="36"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Command="{Binding DataContext.RemovePaymentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                Cursor="Hand"
                                                                ToolTip="Supprimer ce paiement">
                                                            <materialDesign:PackIcon Width="14"
                                                                                     Height="14"
                                                                                     Foreground="#EF4444"
                                                                                     Kind="Close" />
                                                        </Button>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <Grid Margin="0,10,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Style="{StaticResource PosTextBox}"
                                                     Text="{Binding PaymentAmount}"
                                                     ToolTip="Montant à ajouter (laisser vide = reste à payer) — 2 décimales (ex: 1234.56)" />
                                            <Button Grid.Column="1"
                                                    Margin="12,0,0,0"
                                                    Command="{Binding AddPaymentCommand}"
                                                    Style="{StaticResource PosMiniBtn}"
                                                    ToolTip="Ajouter ce montant comme paiement fractionné">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Width="14"
                                                                             Height="14"
                                                                             Margin="0,0,5,0"
                                                                             VerticalAlignment="Center"
                                                                             Foreground="White"
                                                                             Kind="Plus" />
                                                    <TextBlock FontSize="12"
                                                               Foreground="White"
                                                               Text="Ajouter" />
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                        <Grid MinHeight="38"
                                              Margin="0,0,0,12"
                                              ToolTip="Montant restant à payer pour compléter la facture">
                                            <TextBlock VerticalAlignment="Center"
                                                       FontFamily="{StaticResource AppFont}"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"
                                                       Text="Reste à payer" />
                                            <TextBlock HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       FontFamily="{StaticResource AppFont}"
                                                       FontSize="14"
                                                       FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Remaining}"
                                                                         Value="0">
                                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                                <Run Text="{Binding Remaining, StringFormat=N2, Mode=OneWay}" />
                                                <Run Text=" CDF" />
                                            </TextBlock>
                                        </Grid>
                                    </StackPanel>

                                    <!--  ── Simple payment section ──  -->
                                    <StackPanel Visibility="{Binding ShowSplitPayment, Converter={StaticResource InverseBoolConverter}}">
                                        <TextBlock Margin="2,0,0,8"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="Montant reçu"
                                                   ToolTip="Saisissez le montant reçu du client (2 décimales)" />
                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox MinHeight="54"
                                                     Padding="16,14"
                                                     VerticalContentAlignment="Center"
                                                     Background="White"
                                                     FontFamily="{StaticResource AppFont}"
                                                     FontSize="20"
                                                     FontWeight="SemiBold"
                                                     Text="{Binding ReceivedAmount, UpdateSourceTrigger=PropertyChanged}"
                                                     ToolTip="Montant reçu du client en CDF (2 décimales, ex: 1234.56)">
                                                <TextBox.Template>
                                                    <ControlTemplate TargetType="TextBox">
                                                        <Border x:Name="Bd"
                                                                MinHeight="{TemplateBinding MinHeight}"
                                                                Padding="{TemplateBinding Padding}"
                                                                Background="{TemplateBinding Background}"
                                                                BorderThickness="1.5"
                                                                CornerRadius="10"
                                                                SnapsToDevicePixels="True">
                                                            <Border.BorderBrush>
                                                                <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                                                            </Border.BorderBrush>
                                                            <ScrollViewer x:Name="PART_ContentHost"
                                                                          VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsFocused" Value="True">
                                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </TextBox.Template>
                                            </TextBox>
                                            <Button Grid.Column="1"
                                                    Margin="12,0,0,0"
                                                    Command="{Binding SetExactAmountCommand}"
                                                    Style="{StaticResource PosGhostBtn}"
                                                    ToolTip="Remplir automatiquement avec le montant exact à payer">
                                                <TextBlock FontFamily="{StaticResource AppFont}"
                                                           FontSize="12"
                                                           Text="= Exact" />
                                            </Button>
                                        </Grid>

                                        <!--  Quick amount chips  -->
                                        <WrapPanel Margin="0,0,0,12">
                                            <Button Margin="0,0,8,8"
                                                    Command="{Binding SetRoundedAmountCommand}"
                                                    CommandParameter="1000"
                                                    Content="1 000"
                                                    Style="{StaticResource PosChipBtn}"
                                                    ToolTip="Définir le montant reçu à 1 000,00 CDF" />
                                            <Button Margin="0,0,8,8"
                                                    Command="{Binding SetRoundedAmountCommand}"
                                                    CommandParameter="5000"
                                                    Content="5 000"
                                                    Style="{StaticResource PosChipBtn}"
                                                    ToolTip="Définir le montant reçu à 5 000,00 CDF" />
                                            <Button Margin="0,0,8,8"
                                                    Command="{Binding SetRoundedAmountCommand}"
                                                    CommandParameter="10000"
                                                    Content="10 000"
                                                    Style="{StaticResource PosChipBtn}"
                                                    ToolTip="Définir le montant reçu à 10 000,00 CDF" />
                                            <Button Margin="0,0,8,8"
                                                    Command="{Binding SetRoundedAmountCommand}"
                                                    CommandParameter="20000"
                                                    Content="20 000"
                                                    Style="{StaticResource PosChipBtn}"
                                                    ToolTip="Définir le montant reçu à 20 000,00 CDF" />
                                            <Button Margin="0,0,0,8"
                                                    Command="{Binding SetRoundedAmountCommand}"
                                                    CommandParameter="50000"
                                                    Content="50 000"
                                                    Style="{StaticResource PosChipBtn}"
                                                    ToolTip="Définir le montant reçu à 50 000,00 CDF" />
                                        </WrapPanel>
                                    </StackPanel>

                                    <!--  Change  -->
                                    <Border Margin="0,0,0,14"
                                            Padding="20,14"
                                            CornerRadius="12"
                                            ToolTip="Monnaie à rendre au client"
                                            Visibility="{Binding ShowChange, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Border.Background>
                                            <SolidColorBrush Opacity="0.08"
                                                             Color="{StaticResource WarningColor}" />
                                        </Border.Background>
                                        <Grid MinHeight="38">
                                            <StackPanel VerticalAlignment="Center"
                                                        Orientation="Horizontal">
                                                <materialDesign:PackIcon Width="16"
                                                                         Height="16"
                                                                         Margin="0,0,8,0"
                                                                         VerticalAlignment="Center"
                                                                         Foreground="#92400E"
                                                                         Kind="CashRefund" />
                                                <TextBlock FontFamily="{StaticResource AppFont}"
                                                           FontSize="13"
                                                           FontWeight="Medium"
                                                           Foreground="#92400E"
                                                           Text="Monnaie" />
                                            </StackPanel>
                                            <TextBlock HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       FontFamily="{StaticResource AppFont}"
                                                       FontSize="16"
                                                       FontWeight="Bold"
                                                       Foreground="#92400E"
                                                       ToolTip="{Binding ChangeAmount, StringFormat='{}{0:N2} CDF à rendre'}">
                                                <Run Text="{Binding ChangeAmount, StringFormat=N2, Mode=OneWay}" />
                                                <Run Text=" CDF" />
                                            </TextBlock>
                                        </Grid>
                                    </Border>

                                    <!--  Hold + Process buttons  -->
                                    <Grid Margin="0,4,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Button Margin="0,0,12,0"
                                                Command="{Binding RequestHoldCommand}"
                                                Style="{StaticResource PosHoldBtn}"
                                                ToolTip="Mettre cette vente en attente pour y revenir plus tard">
                                            <materialDesign:PackIcon Width="22"
                                                                     Height="22"
                                                                     Foreground="White"
                                                                     Kind="PauseCircleOutline" />
                                        </Button>
                                        <Button Grid.Column="1"
                                                HorizontalContentAlignment="Center"
                                                Command="{Binding ProcessSaleCommand}"
                                                Style="{StaticResource PosProcessBtn}"
                                                ToolTip="Valider et normaliser la vente auprès du dispositif DEF/DGI">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Width="20"
                                                                         Height="20"
                                                                         Margin="0,0,10,0"
                                                                         VerticalAlignment="Center"
                                                                         Foreground="White"
                                                                         Kind="ShieldLockOutline" />
                                                <TextBlock FontFamily="{StaticResource AppFont}"
                                                           FontSize="16"
                                                           FontWeight="Bold"
                                                           Foreground="White"
                                                           Text="ENCAISSER" />
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </StackPanel>

                                <!--  ═══════ AFTER NORMALIZATION ═══════  -->
                                <StackPanel Visibility="{Binding IsNormalized, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Border Margin="0,0,0,14"
                                            Padding="20,16"
                                            CornerRadius="12"
                                            ToolTip="Code de validation DEF/DGI de cette facture">
                                        <Border.Background>
                                            <SolidColorBrush Opacity="0.05"
                                                             Color="{StaticResource SuccessColor}" />
                                        </Border.Background>
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock HorizontalAlignment="Center"
                                                       FontFamily="{StaticResource AppFont}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       Text="CODE DEF/DGI" />
                                            <TextBlock Margin="0,6,0,0"
                                                       HorizontalAlignment="Center"
                                                       FontFamily="Consolas"
                                                       FontSize="13"
                                                       FontWeight="Bold"
                                                       Text="{Binding CodeDEFDGI}"
                                                       ToolTip="{Binding CodeDEFDGI}" />
                                        </StackPanel>
                                    </Border>
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition MinHeight="28" />
                                            <RowDefinition MinHeight="28" />
                                            <RowDefinition MinHeight="28" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="NIM:"
                                                   ToolTip="Numéro d'Identification Machine" />
                                        <TextBlock Grid.Column="1"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   Text="{Binding Nim}"
                                                   ToolTip="{Binding Nim}" />
                                        <TextBlock Grid.Row="1"
                                                   Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="Compteurs:"
                                                   ToolTip="Compteurs de facturation du dispositif" />
                                        <TextBlock Grid.Row="1"
                                                   Grid.Column="1"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   Text="{Binding Counters}"
                                                   ToolTip="{Binding Counters}" />
                                        <TextBlock Grid.Row="2"
                                                   Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="Date DEF:"
                                                   ToolTip="Date et heure enregistrées par le dispositif DEF" />
                                        <TextBlock Grid.Row="2"
                                                   Grid.Column="1"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   Text="{Binding DeviceDateTime}"
                                                   ToolTip="{Binding DeviceDateTime}" />
                                    </Grid>
                                    <Button HorizontalContentAlignment="Center"
                                            Command="{Binding NewSaleCommand}"
                                            Style="{StaticResource PosPrimaryBtn}"
                                            ToolTip="Commencer une nouvelle vente (réinitialiser le panier)">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Width="18"
                                                                     Height="18"
                                                                     Margin="0,0,10,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="White"
                                                                     Kind="FilePlusOutline" />
                                            <TextBlock FontFamily="{StaticResource AppFont}"
                                                       FontSize="14"
                                                       FontWeight="Bold"
                                                       Foreground="White"
                                                       Text="NOUVELLE VENTE" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <!--  Reprint  -->
                                <Button Margin="0,14,0,0"
                                        Padding="18,12"
                                        HorizontalContentAlignment="Center"
                                        Command="{Binding ReprintReceiptCommand}"
                                        Style="{StaticResource PosGhostBtn}"
                                        ToolTip="Imprimer un duplicata du reçu sur l'imprimante thermique"
                                        Visibility="{Binding HasThermalPrinter, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="15"
                                                                 Height="15"
                                                                 Margin="0,0,8,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 Kind="PrinterOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Text="Réimprimer (Duplicata)" />
                                    </StackPanel>
                                </Button>

                                <!--  Printer indicator  -->
                                <Border Margin="0,12,0,0"
                                        Padding="10,6"
                                        HorizontalAlignment="Center"
                                        CornerRadius="8"
                                        ToolTip="{Binding ThermalPrinterName}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Opacity="0.06"
                                                                     Color="{StaticResource SuccessColor}" />
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasThermalPrinter}"
                                                             Value="False">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <SolidColorBrush Opacity="0.06"
                                                                             Color="{StaticResource ErrorColor}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="6"
                                                 Height="6"
                                                 Margin="0,0,6,0"
                                                 VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="#10B981" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasThermalPrinter}"
                                                                     Value="False">
                                                            <Setter Property="Fill" Value="#EF4444" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="9"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   Text="{Binding ThermalPrinterName}"
                                                   ToolTip="{Binding ThermalPrinterName}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>

                </Grid>
            </Border>

            <!--  ══════════════════════════════════════════════════════  -->
            <!--  OVERLAY PANELS  -->
            <!--  ══════════════════════════════════════════════════════  -->

            <!--  CLIENT PANEL ★  -->
            <Border Margin="0"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowClientPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Header  -->
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource PrimaryBrush}"
                                                         Kind="AccountOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Text="Client"
                                           ToolTip="Panneau de gestion du client" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleClientPanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>

                    <!--  Scrollable form  -->
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,20">
                            <!--  Search  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Rechercher"
                                       ToolTip="Rechercher un client existant" />
                            <Grid Margin="0,0,0,18">
                                <TextBox x:Name="PosClientSearch"
                                         Style="{StaticResource PosTextBox}"
                                         Text="{Binding ClientSearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                                         ToolTip="Tapez le nom, NIF ou téléphone du client pour le rechercher" />
                                <Popup Width="{Binding ActualWidth, ElementName=PosClientSearch}"
                                       AllowsTransparency="True"
                                       IsOpen="{Binding IsClientSearchOpen, Mode=TwoWay}"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=PosClientSearch}"
                                       PopupAnimation="Fade"
                                       StaysOpen="False">
                                    <Border MaxHeight="260"
                                            Background="White"
                                            BorderThickness="1"
                                            CornerRadius="0,0,12,12"
                                            Effect="{StaticResource PopupShadow}">
                                        <Border.BorderBrush>
                                            <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                                        </Border.BorderBrush>
                                        <ScrollViewer PanningMode="VerticalOnly"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding ClientSearchResults}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Command="{Binding DataContext.SelectClientCommand, RelativeSource={RelativeSource AncestorType=Popup}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource PopupItemBtn}"
                                                                ToolTip="{Binding Name}">
                                                            <StackPanel>
                                                                <TextBlock FontFamily="{StaticResource AppFont}"
                                                                           FontSize="14"
                                                                           FontWeight="Medium"
                                                                           Text="{Binding Name}"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           ToolTip="{Binding Name}" />
                                                                <TextBlock Margin="0,2,0,0"
                                                                           FontFamily="Consolas"
                                                                           FontSize="11"
                                                                           Foreground="{StaticResource TextMutedBrush}"
                                                                           ToolTip="Type de client et NIF">
                                                                    <Run Text="{Binding Type}" />
                                                                    <Run Text=" · NIF: " />
                                                                    <Run Text="{Binding NIF, TargetNullValue='-'}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </Button>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>

                            <!--  Type  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Type client"
                                       ToolTip="Type de client (personne physique, morale, etc.)" />
                            <ComboBox Margin="0,0,0,18"
                                      ItemsSource="{Binding ClientTypes}"
                                      SelectedItem="{Binding SelectedClientType}"
                                      Style="{StaticResource PosComboBox}"
                                      ToolTip="Sélectionner le type de client" />

                            <!--  NIF  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="NIF"
                                       ToolTip="Numéro d'Identification Fiscale du client" />
                            <TextBox Margin="0,0,0,18"
                                     Style="{StaticResource PosTextBox}"
                                     Text="{Binding ClientNIF}"
                                     ToolTip="Saisir le NIF du client" />

                            <!--  Name  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Nom / Dénomination"
                                       ToolTip="Nom complet ou dénomination sociale du client" />
                            <TextBox Margin="0,0,0,18"
                                     Style="{StaticResource PosTextBox}"
                                     Text="{Binding ClientName}"
                                     ToolTip="Saisir le nom ou la dénomination du client" />

                            <!--  Phone  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Téléphone"
                                       ToolTip="Numéro de téléphone du client" />
                            <TextBox Margin="0,0,0,18"
                                     Style="{StaticResource PosTextBox}"
                                     Text="{Binding ClientPhone}"
                                     ToolTip="Saisir le numéro de téléphone du client" />

                            <!--  Address  -->
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Adresse"
                                       ToolTip="Adresse physique du client" />
                            <TextBox Style="{StaticResource PosTextBox}"
                                     Text="{Binding ClientAddress}"
                                     ToolTip="Saisir l'adresse du client" />
                        </StackPanel>
                    </ScrollViewer>

                    <!--  Footer buttons  -->
                    <Border Grid.Row="2"
                            Padding="28,16,28,22"
                            BorderThickness="0,1,0,0">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                        </Border.BorderBrush>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Command="{Binding SaveClientInlineCommand}"
                                    Style="{StaticResource PosSuccessBtn}"
                                    ToolTip="Enregistrer les informations du client">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="15"
                                                             Height="15"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="#065F46"
                                                             Kind="ContentSave" />
                                    <TextBlock FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="#065F46"
                                               Text="Enregistrer" />
                                </StackPanel>
                            </Button>
                            <Button Grid.Column="2"
                                    Command="{Binding ClearClientCommand}"
                                    Style="{StaticResource PosDangerBtn}"
                                    ToolTip="Vider tous les champs client et dissocier le client de la vente">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="14"
                                                             Height="14"
                                                             Margin="0,0,6,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="#DC2626"
                                                             Kind="Close" />
                                    <TextBlock FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="#DC2626"
                                               Text="Vider" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!--  CREDIT NOTE PANEL  -->
            <Border Margin="0"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowCreditNotePanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource AccentBrush}"
                                                         Kind="FileUndoOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource AccentBrush}"
                                           Text="Avoir"
                                           ToolTip="Panneau de gestion des avoirs (notes de crédit)" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleCreditNotePanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,24">
                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Nature"
                                       ToolTip="Nature de l'avoir (retour, remise, erreur, etc.)" />
                            <ComboBox Margin="0,0,0,18"
                                      ItemsSource="{Binding CreditNoteNatures}"
                                      SelectedItem="{Binding SelectedCreditNoteNature}"
                                      Style="{StaticResource PosComboBox}"
                                      ToolTip="Sélectionner la nature de l'avoir" />

                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="Code DEF/DGI original"
                                       ToolTip="Code DEF/DGI de la facture originale à annuler" />
                            <Grid Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox FontFamily="Consolas"
                                         IsEnabled="{Binding IsRRR, Converter={StaticResource InverseBoolConverter}}"
                                         Style="{StaticResource PosTextBox}"
                                         Text="{Binding OriginalReference, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Saisir le code DEF/DGI de la facture originale" />
                                <Button Grid.Column="1"
                                        Margin="10,0,0,0"
                                        Command="{Binding LookupOriginalInvoiceCommand}"
                                        Style="{StaticResource PosMiniBtn}"
                                        ToolTip="Rechercher et charger la facture originale à partir du code"
                                        Visibility="{Binding RequiresOriginalLookup, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="14"
                                                                 Height="14"
                                                                 Margin="0,0,6,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="White"
                                                                 Kind="Magnify" />
                                        <TextBlock FontSize="12"
                                                   Foreground="White"
                                                   Text="Charger" />
                                    </StackPanel>
                                </Button>
                            </Grid>

                            <Border Margin="0,0,0,18"
                                    Padding="16,14"
                                    CornerRadius="12"
                                    ToolTip="Facture originale chargée avec succès"
                                    Visibility="{Binding IsOriginalLoaded, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Border.Background>
                                    <SolidColorBrush Opacity="0.06"
                                                     Color="{StaticResource SuccessColor}" />
                                </Border.Background>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="15"
                                                                 Height="15"
                                                                 Margin="0,0,8,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="#065F46"
                                                                 Kind="CheckCircleOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="12"
                                                   FontWeight="Bold"
                                                   Foreground="#065F46"
                                                   Text="Facture chargée" />
                                    </StackPanel>
                                    <TextBlock Margin="23,4,0,0"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Text="{Binding OriginalInvoiceSummary}"
                                               TextWrapping="Wrap"
                                               ToolTip="{Binding OriginalInvoiceSummary}" />
                                </StackPanel>
                            </Border>

                            <ItemsControl ItemsSource="{Binding CreditNoteSelections}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border MinHeight="56"
                                                Padding="8,12"
                                                BorderThickness="0,0,0,1"
                                                ToolTip="{Binding DisplayName}">
                                            <Border.BorderBrush>
                                                <SolidColorBrush Color="{StaticResource BorderLightColor}" />
                                            </Border.BorderBrush>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="38" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="95" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <CheckBox VerticalAlignment="Center"
                                                          IsChecked="{Binding IsSelected}"
                                                          ToolTip="Cocher pour inclure cet article dans l'avoir" />
                                                <StackPanel Grid.Column="1"
                                                            VerticalAlignment="Center">
                                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                                               FontSize="12"
                                                               FontWeight="Medium"
                                                               Text="{Binding DisplayName}"
                                                               ToolTip="{Binding DisplayName}" />
                                                    <TextBlock Margin="0,2,0,0"
                                                               FontFamily="{StaticResource AppFont}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               Text="{Binding DisplayQuantity}"
                                                               ToolTip="{Binding DisplayQuantity}" />
                                                </StackPanel>
                                                <TextBox Grid.Column="2"
                                                         MinHeight="40"
                                                         Margin="6,0"
                                                         Padding="10,6"
                                                         FontSize="13"
                                                         Style="{StaticResource PosTextBox}"
                                                         Text="{Binding SelectedQuantity, UpdateSourceTrigger=PropertyChanged, StringFormat=N3}"
                                                         ToolTip="Quantité à retourner (3 décimales)" />
                                                <TextBlock Grid.Column="3"
                                                           Margin="6,0,0,0"
                                                           VerticalAlignment="Center"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="11"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource PrimaryBrush}"
                                                           Text="{Binding DisplayPrice}"
                                                           ToolTip="{Binding DisplayPrice}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Margin="0,18,0,0"
                                    HorizontalContentAlignment="Center"
                                    Command="{Binding AddAllSelectedCreditNoteLinesCommand}"
                                    Style="{StaticResource PosAccentBtn}"
                                    ToolTip="Ajouter tous les articles cochés au panier de l'avoir">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="15"
                                                             Height="15"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="White"
                                                             Kind="Plus" />
                                    <TextBlock FontSize="13"
                                               Foreground="White"
                                               Text="Ajouter sélection" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  ADVANCE PANEL  -->
            <Border MaxHeight="500"
                    Margin="0"
                    VerticalAlignment="Top"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowAdvancePanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="#7C3AED"
                                                         Kind="CashClock" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Foreground="#7C3AED"
                                           Text="Acompte"
                                           ToolTip="Panneau de gestion des acomptes" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleAdvancePanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,24">

                            <!--  🆕 Montant de l'acompte reçu  -->
                            <Border Margin="0,0,0,20"
                                    Padding="16,14"
                                    Background="#F5F3FF"
                                    BorderThickness="2"
                                    CornerRadius="12">
                                <Border.BorderBrush>
                                    <SolidColorBrush Opacity="0.4"
                                                     Color="#7C3AED" />
                                </Border.BorderBrush>
                                <StackPanel>
                                    <StackPanel Margin="0,0,0,8"
                                                Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="16"
                                                                 Height="16"
                                                                 Margin="0,0,6,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="#7C3AED"
                                                                 Kind="CashCheck" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="13"
                                                   FontWeight="Bold"
                                                   Foreground="#7C3AED"
                                                   Text="Montant reçu *" />
                                    </StackPanel>
                                    <TextBox FontSize="18"
                                             FontWeight="SemiBold"
                                             IsEnabled="{Binding IsNormalized, Converter={StaticResource InverseBoolConverter}}"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding AdvanceAmountInput, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="Montant exact remis par le client pour cet acompte (ex : 50000.00)" />
                                    <StackPanel Margin="0,8,0,0"
                                                Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="12"
                                                                 Height="12"
                                                                 Margin="0,0,5,0"
                                                                 VerticalAlignment="Top"
                                                                 Foreground="#6D28D9"
                                                                 Kind="InformationOutline" />
                                        <TextBlock FontFamily="{StaticResource AppFont}"
                                                   FontSize="10"
                                                   Foreground="#6D28D9"
                                                   Text="Le panier représente la commande totale. Ce champ = ce que le client verse maintenant (2 décimales)."
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <TextBlock Style="{StaticResource PosFieldLabel}"
                                       Text="ID du groupe"
                                       ToolTip="Identifiant du groupe d'acomptes" />
                            <TextBox Margin="0,0,0,16"
                                     FontFamily="Consolas"
                                     Style="{StaticResource PosTextBox}"
                                     Text="{Binding AdvanceGroupId, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Saisir ou afficher l'identifiant du groupe d'acomptes" />
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Button Command="{Binding CreateNewAdvanceGroupCommand}"
                                        Style="{StaticResource PosPurpleBtn}"
                                        ToolTip="Créer un nouveau groupe d'acomptes">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="14"
                                                                 Height="14"
                                                                 Margin="0,0,6,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="White"
                                                                 Kind="PlusCircleOutline" />
                                        <TextBlock Foreground="White"
                                                   Text="Nouveau" />
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="2"
                                        Command="{Binding LoadAdvanceGroupCommand}"
                                        Style="{StaticResource PosGhostBtn}"
                                        ToolTip="Charger un groupe d'acomptes existant">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="14"
                                                                 Height="14"
                                                                 Margin="0,0,6,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 Kind="FolderOpenOutline" />
                                        <TextBlock FontSize="12"
                                                   Text="Charger" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                            <ItemsControl ItemsSource="{Binding PreviousAdvances}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border MinHeight="48"
                                                Margin="0,4"
                                                Padding="16,12"
                                                Background="#F5F3FF"
                                                CornerRadius="10"
                                                ToolTip="{Binding InvoiceNumber}">
                                            <Grid>
                                                <TextBlock VerticalAlignment="Center"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="12"
                                                           ToolTip="{Binding InvoiceNumber}">
                                                    <Run FontWeight="SemiBold"
                                                         Text="{Binding InvoiceNumber}" />
                                                    <Run Text=" — " />
                                                    <Run Text="{Binding Date, StringFormat=dd/MM/yyyy}" />
                                                </TextBlock>
                                                <TextBlock HorizontalAlignment="Right"
                                                           VerticalAlignment="Center"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           Foreground="#7C3AED"
                                                           ToolTip="{Binding Amount, StringFormat='{}{0:N2} CDF'}">
                                                    <Run Text="{Binding Amount, StringFormat=N2}" />
                                                    <Run FontSize="10"
                                                         Text=" CDF" />
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  COMMENT PANEL  -->
            <Border MaxHeight="540"
                    Margin="0"
                    VerticalAlignment="Top"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowCommentPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource PrimaryBrush}"
                                                         Kind="CommentTextOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Text="Commentaires"
                                           ToolTip="Ajouter des commentaires à la facture (lignes A à H)" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleCommentPanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,24">
                            <TextBlock Margin="2,0,0,8"
                                       FontFamily="{StaticResource AppFont}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Text="{Binding CommentALabel}"
                                       ToolTip="Commentaire principal de la facture" />
                            <TextBox Margin="0,0,0,18"
                                     Style="{StaticResource PosTextBox}"
                                     Text="{Binding CommentA, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Saisir le commentaire principal (ligne A)" />
                            <Expander Padding="0,8,0,0"
                                      FontFamily="{StaticResource AppFont}"
                                      FontSize="12"
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Header="Lignes B–H"
                                      ToolTip="Développer pour voir les lignes de commentaires supplémentaires">
                                <StackPanel Margin="0,12,0,0">
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="B"
                                               ToolTip="Ligne de commentaire B" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentB}"
                                             ToolTip="Saisir le commentaire ligne B" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="C"
                                               ToolTip="Ligne de commentaire C" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentC}"
                                             ToolTip="Saisir le commentaire ligne C" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="D"
                                               ToolTip="Ligne de commentaire D" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentD}"
                                             ToolTip="Saisir le commentaire ligne D" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="E"
                                               ToolTip="Ligne de commentaire E" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentE}"
                                             ToolTip="Saisir le commentaire ligne E" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="F"
                                               ToolTip="Ligne de commentaire F" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentF}"
                                             ToolTip="Saisir le commentaire ligne F" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="G"
                                               ToolTip="Ligne de commentaire G" />
                                    <TextBox MinHeight="40"
                                             Margin="0,0,0,12"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentG}"
                                             ToolTip="Saisir le commentaire ligne G" />
                                    <TextBlock Style="{StaticResource PosFieldLabel}"
                                               Text="H"
                                               ToolTip="Ligne de commentaire H" />
                                    <TextBox MinHeight="40"
                                             Padding="12,8"
                                             Style="{StaticResource PosTextBox}"
                                             Text="{Binding CommentH}"
                                             ToolTip="Saisir le commentaire ligne H" />
                                </StackPanel>
                            </Expander>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  PENDING INVOICES PANEL  -->
            <Border Margin="0"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowPendingPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource WarningBrush}"
                                                         Kind="TimerSandEmpty" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Text="Factures en attente"
                                           ToolTip="Factures envoyées au dispositif DEF mais pas encore confirmées" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding TogglePendingPanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,24">
                            <Button Margin="0,0,0,16"
                                    HorizontalContentAlignment="Center"
                                    Command="{Binding CheckPendingInvoicesCommand}"
                                    Style="{StaticResource PosMiniBtn}"
                                    ToolTip="Interroger le dispositif DEF pour vérifier les factures en attente">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="15"
                                                             Height="15"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="White"
                                                             Kind="Refresh" />
                                    <TextBlock FontSize="13"
                                               Foreground="White"
                                               Text="Vérifier le dispositif" />
                                </StackPanel>
                            </Button>
                            <ItemsControl ItemsSource="{Binding PendingInvoices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border MinHeight="56"
                                                Margin="0,4"
                                                Padding="16,14"
                                                BorderThickness="1"
                                                CornerRadius="10"
                                                ToolTip="{Binding Uid}">
                                            <Border.Background>
                                                <SolidColorBrush Opacity="0.05"
                                                                 Color="{StaticResource WarningColor}" />
                                            </Border.Background>
                                            <Border.BorderBrush>
                                                <SolidColorBrush Opacity="0.2"
                                                                 Color="{StaticResource WarningColor}" />
                                            </Border.BorderBrush>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontFamily="Consolas"
                                                               FontSize="12"
                                                               FontWeight="SemiBold"
                                                               ToolTip="{Binding Uid}">
                                                        <Run Text="UID: " /><Run Text="{Binding Uid}" />
                                                    </TextBlock>
                                                    <TextBlock Margin="0,2,0,0"
                                                               FontFamily="{StaticResource AppFont}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               ToolTip="{Binding Date, StringFormat=dd/MM/yyyy}">
                                                        <Run Text="{Binding Date, StringFormat=dd/MM/yyyy}" />
                                                    </TextBlock>
                                                </StackPanel>
                                                <Button Grid.Column="1"
                                                        Command="{Binding DataContext.CancelPendingInvoiceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding Uid}"
                                                        Style="{StaticResource PosDangerBtn}"
                                                        ToolTip="Annuler cette facture en attente">
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Width="12"
                                                                                 Height="12"
                                                                                 Margin="0,0,5,0"
                                                                                 VerticalAlignment="Center"
                                                                                 Foreground="#DC2626"
                                                                                 Kind="Close" />
                                                        <TextBlock FontSize="12"
                                                                   Foreground="#DC2626"
                                                                   Text="Annuler" />
                                                    </StackPanel>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox FontFamily="Consolas"
                                         Style="{StaticResource PosTextBox}"
                                         Text="{Binding CancelUid, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Saisir manuellement l'UID de la facture à annuler" />
                                <Button Grid.Column="1"
                                        Margin="10,0,0,0"
                                        Command="{Binding CancelPendingInvoiceCommand}"
                                        Style="{StaticResource PosAccentBtn}"
                                        ToolTip="Annuler la facture en attente avec l'UID saisi">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Width="14"
                                                                 Height="14"
                                                                 Margin="0,0,6,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="White"
                                                                 Kind="TrashCanOutline" />
                                        <TextBlock FontSize="12"
                                                   Foreground="White"
                                                   Text="Annuler" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                            <Border Margin="0,12,0,0"
                                    Padding="14,10"
                                    CornerRadius="10"
                                    ToolTip="{Binding PendingStatusMessage}"
                                    Visibility="{Binding ShowPendingSuccess, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Border.Background>
                                    <SolidColorBrush Opacity="0.06"
                                                     Color="{StaticResource SuccessColor}" />
                                </Border.Background>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="14"
                                                             Height="14"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="{StaticResource SuccessBrush}"
                                                             Kind="CheckCircleOutline" />
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource SuccessBrush}"
                                               Text="{Binding PendingStatusMessage}"
                                               TextWrapping="Wrap"
                                               ToolTip="{Binding PendingStatusMessage}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0"
                                    Padding="14,10"
                                    CornerRadius="10"
                                    ToolTip="{Binding PendingStatusMessage}"
                                    Visibility="{Binding ShowPendingError, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Border.Background>
                                    <SolidColorBrush Opacity="0.06"
                                                     Color="{StaticResource ErrorColor}" />
                                </Border.Background>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="14"
                                                             Height="14"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="{StaticResource ErrorBrush}"
                                                             Kind="AlertCircleOutline" />
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource ErrorBrush}"
                                               Text="{Binding PendingStatusMessage}"
                                               TextWrapping="Wrap"
                                               ToolTip="{Binding PendingStatusMessage}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  DISCOUNT PANEL  -->
            <Border MaxHeight="500"
                    Margin="0"
                    VerticalAlignment="Top"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowDiscountPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource OverlayHeader}">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="22"
                                                         Height="22"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="{StaticResource AccentBrush}"
                                                         Kind="PercentOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Text="Remise"
                                           ToolTip="Appliquer une remise sur l'article sélectionné" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleDiscountPanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,20,28,24">
                            <Border Margin="0,0,0,20"
                                    Padding="16,14"
                                    Background="{StaticResource BackgroundBrush}"
                                    CornerRadius="10"
                                    ToolTip="Article sur lequel la remise sera appliquée">
                                <StackPanel>
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="13"
                                               FontWeight="Medium"
                                               Text="{Binding SelectedCartItem.Name}"
                                               ToolTip="{Binding SelectedCartItem.Name}" />
                                    <TextBlock Margin="0,3,0,0"
                                               FontFamily="{StaticResource AppFont}"
                                               FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               ToolTip="Quantité et montant actuel de l'article">
                                        <Run Text="{Binding SelectedCartItem.QuantityDisplay, Mode=OneWay}" />
                                        <Run Text=" → " />
                                        <Run Text="{Binding SelectedCartItem.AmountTTC, StringFormat=N2, Mode=OneWay}" />
                                        <Run Text=" CDF" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                            <WrapPanel Margin="0,0,0,20">
                                <Button Margin="0,0,10,10"
                                        Command="{Binding ApplyQuickDiscountCommand}"
                                        CommandParameter="5"
                                        Content="5 %"
                                        Style="{StaticResource PosDiscountChip}"
                                        ToolTip="Appliquer une remise de 5%" />
                                <Button Margin="0,0,10,10"
                                        Command="{Binding ApplyQuickDiscountCommand}"
                                        CommandParameter="10"
                                        Content="10 %"
                                        Style="{StaticResource PosDiscountChip}"
                                        ToolTip="Appliquer une remise de 10%" />
                                <Button Margin="0,0,10,10"
                                        Command="{Binding ApplyQuickDiscountCommand}"
                                        CommandParameter="15"
                                        Content="15 %"
                                        Style="{StaticResource PosDiscountChip}"
                                        ToolTip="Appliquer une remise de 15%" />
                                <Button Margin="0,0,0,10"
                                        Command="{Binding ApplyQuickDiscountCommand}"
                                        CommandParameter="20"
                                        Content="20 %"
                                        Style="{StaticResource PosDiscountChip}"
                                        ToolTip="Appliquer une remise de 20%" />
                            </WrapPanel>
                            <Grid Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox FontSize="16"
                                         Style="{StaticResource PosTextBox}"
                                         Text="{Binding CustomDiscountValue, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Saisir la valeur de la remise personnalisée (2 décimales si CDF)" />
                                <StackPanel Grid.Column="1"
                                            Margin="12,0"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                    <RadioButton MinHeight="40"
                                                 Margin="0,0,12,0"
                                                 Content=" %"
                                                 FontFamily="{StaticResource AppFont}"
                                                 FontSize="13"
                                                 IsChecked="{Binding IsPercentDiscount}"
                                                 ToolTip="Remise en pourcentage" />
                                    <RadioButton MinHeight="40"
                                                 Content=" CDF"
                                                 FontFamily="{StaticResource AppFont}"
                                                 FontSize="13"
                                                 ToolTip="Remise en montant fixe (CDF, 2 décimales)" />
                                </StackPanel>
                                <Button Grid.Column="2"
                                        Padding="18,10"
                                        Command="{Binding ApplyCustomDiscountCommand}"
                                        Content="OK"
                                        Style="{StaticResource PosAccentBtn}"
                                        ToolTip="Appliquer la remise personnalisée" />
                            </Grid>
                            <Button Margin="0,6,0,0"
                                    HorizontalContentAlignment="Center"
                                    Command="{Binding RemoveItemDiscountCommand}"
                                    Style="{StaticResource PosDangerBtn}"
                                    ToolTip="Supprimer la remise appliquée à cet article"
                                    Visibility="{Binding SelectedCartItem.HasDiscount, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="14"
                                                             Height="14"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="#DC2626"
                                                             Kind="TrashCanOutline" />
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="13"
                                               Foreground="#DC2626"
                                               Text="Supprimer la remise" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  HELD PANEL  -->
            <Border Margin="0"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource PosOverlayCard}"
                    Visibility="{Binding ShowHeldPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                <DockPanel>
                    <Border Padding="24,18"
                            CornerRadius="16,16,0,0"
                            DockPanel.Dock="Top">
                        <Border.Background>
                            <SolidColorBrush Opacity="0.1"
                                             Color="{StaticResource WarningColor}" />
                        </Border.Background>
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Width="20"
                                                         Height="20"
                                                         Margin="0,0,10,0"
                                                         VerticalAlignment="Center"
                                                         Foreground="#92400E"
                                                         Kind="PauseCircleOutline" />
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="#92400E"
                                           Text="En attente"
                                           ToolTip="Ventes mises en pause pour y revenir plus tard" />
                                <Border Margin="12,0,0,0"
                                        Padding="10,3"
                                        VerticalAlignment="Center"
                                        Background="#92400E"
                                        CornerRadius="10"
                                        ToolTip="Nombre de ventes en attente">
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="12"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               Text="{Binding HeldCount}" />
                                </Border>
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding ToggleHeldPanelCommand}"
                                    Style="{StaticResource PosCloseBtn}" />
                        </Grid>
                    </Border>
                    <ScrollViewer Padding="12,10"
                                  PanningMode="VerticalOnly"
                                  VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding HeldTransactions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border MinHeight="72"
                                            Margin="4,4"
                                            Padding="18,16"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            ToolTip="{Binding Label}">
                                        <Border.Background>
                                            <SolidColorBrush Opacity="0.04"
                                                             Color="{StaticResource WarningColor}" />
                                        </Border.Background>
                                        <Border.BorderBrush>
                                            <SolidColorBrush Opacity="0.2"
                                                             Color="{StaticResource WarningColor}" />
                                        </Border.BorderBrush>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock MaxWidth="200"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           Text="{Binding Label}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding Label}" />
                                                <TextBlock Margin="0,3,0,0"
                                                           FontFamily="{StaticResource AppFont}"
                                                           FontSize="11"
                                                           Foreground="{StaticResource TextMutedBrush}"
                                                           ToolTip="Nombre d'articles et montant total">
                                                    <Run Text="{Binding ItemCount, Mode=OneWay}" />
                                                    <Run Text=" art. · " />
                                                    <Run Text="{Binding TotalDisplay, Mode=OneWay}" />
                                                    <Run Text=" CDF" />
                                                </TextBlock>
                                                <StackPanel Margin="0,3,0,0"
                                                            Orientation="Horizontal">
                                                    <materialDesign:PackIcon Width="11"
                                                                             Height="11"
                                                                             Margin="0,0,4,0"
                                                                             VerticalAlignment="Center"
                                                                             Foreground="{StaticResource TextMutedBrush}"
                                                                             Kind="ClockOutline" />
                                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource TextMutedBrush}"
                                                               ToolTip="Heure de mise en attente">
                                                        <Run Text="{Binding TimeDisplay, Mode=OneWay}" />
                                                    </TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                            <Button Grid.Column="1"
                                                    Width="48"
                                                    Height="48"
                                                    Margin="10,0,8,0"
                                                    VerticalAlignment="Center"
                                                    Command="{Binding DataContext.RecallHeldSaleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Cursor="Hand"
                                                    ToolTip="Reprendre cette vente en attente">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border x:Name="Bg"
                                                                            Width="48"
                                                                            Height="48"
                                                                            CornerRadius="12">
                                                                        <Border.Background>
                                                                            <SolidColorBrush x:Name="BgBr"
                                                                                             Color="#10B981" />
                                                                        </Border.Background>
                                                                        <materialDesign:PackIcon Width="20"
                                                                                                 Height="20"
                                                                                                 HorizontalAlignment="Center"
                                                                                                 VerticalAlignment="Center"
                                                                                                 Foreground="White"
                                                                                                 Kind="Play" />
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <EventTrigger RoutedEvent="MouseEnter">
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <ColorAnimation Storyboard.TargetName="BgBr"
                                                                                                    Storyboard.TargetProperty="Color"
                                                                                                    To="#059669"
                                                                                                    Duration="0:0:0.15" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </EventTrigger>
                                                                        <EventTrigger RoutedEvent="MouseLeave">
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <ColorAnimation Storyboard.TargetName="BgBr"
                                                                                                    Storyboard.TargetProperty="Color"
                                                                                                    To="#10B981"
                                                                                                    Duration="0:0:0.18" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </EventTrigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            <Button Grid.Column="2"
                                                    Width="40"
                                                    Height="40"
                                                    VerticalAlignment="Center"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.DeleteHeldSaleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Cursor="Hand"
                                                    ToolTip="Supprimer définitivement cette vente en attente">
                                                <materialDesign:PackIcon Width="16"
                                                                         Height="16"
                                                                         Foreground="{StaticResource TextMutedBrush}"
                                                                         Kind="TrashCanOutline"
                                                                         Opacity="0.6" />
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!--  HOLD DIALOG  -->
            <Grid Visibility="{Binding ShowHoldDialog, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border Background="#80000000"
                        CornerRadius="16" />
                <Border Margin="32"
                        Padding="32"
                        VerticalAlignment="Center"
                        Background="{StaticResource CardBrush}"
                        CornerRadius="16">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="28"
                                          Opacity="0.2"
                                          ShadowDepth="4"
                                          Color="#0D2B3E" />
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Margin="0,0,0,18"
                                    Orientation="Horizontal">
                            <materialDesign:PackIcon Width="22"
                                                     Height="22"
                                                     Margin="0,0,10,0"
                                                     VerticalAlignment="Center"
                                                     Foreground="#D97706"
                                                     Kind="PauseCircleOutline" />
                            <TextBlock FontFamily="{StaticResource AppFont}"
                                       FontSize="17"
                                       FontWeight="Bold"
                                       Text="Mettre en attente"
                                       ToolTip="Mettre la vente en cours de côté pour y revenir ultérieurement" />
                        </StackPanel>
                        <TextBlock Margin="2,0,0,8"
                                   Style="{StaticResource PosFieldLabel}"
                                   Text="Raison (optionnel) :"
                                   ToolTip="Indiquez une raison pour identifier cette vente en attente" />
                        <TextBox Margin="0,0,0,22"
                                 MaxLength="100"
                                 Style="{StaticResource PosTextBox}"
                                 Text="{Binding HoldReason, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Saisir une raison pour cette mise en attente (ex: client revient, attente de prix...)" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Padding="16,14"
                                    Command="{Binding CancelHoldCommand}"
                                    Style="{StaticResource PosGhostBtn}"
                                    ToolTip="Annuler et revenir à la vente en cours">
                                <TextBlock FontFamily="{StaticResource AppFont}"
                                           FontSize="14"
                                           Text="Annuler" />
                            </Button>
                            <Button Grid.Column="2"
                                    Command="{Binding HoldCurrentSaleCommand}"
                                    Style="{StaticResource PosHoldBtn}"
                                    ToolTip="Confirmer la mise en attente de cette vente">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="16"
                                                             Height="16"
                                                             Margin="0,0,8,0"
                                                             VerticalAlignment="Center"
                                                             Foreground="White"
                                                             Kind="PauseCircleOutline" />
                                    <TextBlock FontFamily="{StaticResource AppFont}"
                                               FontSize="14"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               Text="Confirmer" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>